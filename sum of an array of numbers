//Using the forEach function defined for an array, find the sum of the array of numbers. [function add_all(arr) {...}]

var arr = [1,2,3,4,5]
num=0
function add_all(arr){
    arr.forEach(function(e){num+=e;})
    return num;
}
add_all(arr)
